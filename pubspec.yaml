name: wakenya_tech
description: A new Flutter project
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  # Firebase – consistent versions
  firebase_core: ^3.3.0
  firebase_auth: ^5.1.0
  cloud_firestore: ^5.4.0
  firebase_storage: ^12.1.0

  cupertino_icons: ^1.0.8
  dio: ^5.9.0
  connectivity_plus: ^5.0.2
  flutter_riverpod: ^2.6.1
  go_router: ^16.0.0
  local_auth: ^2.3.0
  intl: ^0.20.2
  fl_chart: ^1.0.0
  image_picker: ^1.1.2
  animate_do: ^4.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true